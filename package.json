{
  "name": "support-desk",
  "private": true,
  "version": "0.1.0",
  "description": "Support-desk dashboard (Node/Express API + React/Vite frontend)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
